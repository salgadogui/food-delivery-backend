# json.extract! order, :id, :user_id, :store_id, :total_value, :created_at, :updated_at

# json.order_items order.order_items do |order_item|
#   json.extract! order_item, :id, :product_id, :quantity, :price, :created_at, :updated_at
# end

# json.url store_order_url(order.store, order, format: :json)
